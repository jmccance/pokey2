# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /socket              @pokey.websocket.SocketController.socket
# Map static resources from the /public folder to the /assets URL path
GET     /                    @pokey.assets.SessionAssetController.assets(path="/public", file="index.html")
GET     /*file               @pokey.assets.SessionAssetController.assets(path="/public", file)
